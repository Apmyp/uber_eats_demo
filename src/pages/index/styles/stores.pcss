.stores {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: flex-start;

    &__item {
        margin-right: 36px;
        line-height: 1;

        &:last-of-type {
            margin-right: 0;
        }
    }
}